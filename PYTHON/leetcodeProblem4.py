def findMedianSortedArrays(nums1, nums2):
    
    totalSize = len(nums1) + len(nums2)
    mid = totalSize//2
    if mid % 2 == 0:
        for index in range(0, mid):
            pass
            nums1
